# Store — LLM template (lean, improving)

Live Qwen loads **`core_skill.md`** + **`sop.md`** only.

## Rules

1. Exactly those two live doctrine files — improve, do not inflate.
2. Evidence before edit — promote from `model_training/tick_data/` review.
3. Bump version in the HTML comment when you edit.
4. Process for training / curriculum: **`../model_training/CURRICULUM_AND_DATA_PREP.md`** only — do not create parallel docs.

Wrong: append a new bullet every day.  
Right: one sharper rule replaces three vague ones.
